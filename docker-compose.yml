services:
  app:
    container_name: opennexus
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3090:80'
    restart: unless-stopped
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true
