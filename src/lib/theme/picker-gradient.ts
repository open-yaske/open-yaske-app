/**
 * カラーピッカー専用: 虹色グラデーション
 * ColorPicker で「カスタム色未選択時の見本」として表示する。
 * 動的なグラデーション用の色リテラルを集約する。
 */
export const RAINBOW_GRADIENT =
	'linear-gradient(135deg, #ef4444 0%, #f59e0b 25%, #10b981 50%, #3b82f6 75%, #8b5cf6 100%)';

/** カスタム色未指定時の HEX 初期値 (黒) */
export const DEFAULT_CUSTOM_HEX = '#000000';
